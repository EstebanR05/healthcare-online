{
  "name": "db",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build index.ts --outdir dist --target bun",
    "test": "bun test"
  },
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "^6.13.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
